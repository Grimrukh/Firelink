// doctest entry point. Keeping it in its own TU so that individual test
// files stay compile-clean (they only #include <doctest/doctest.h>).

#define DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN
#include <doctest/doctest.h>
